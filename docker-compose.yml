services:
  db:
    image: postgres:16-alpine
    container_name: pethood-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: pethood
      POSTGRES_PASSWORD: pethood
      POSTGRES_DB: pethood
    ports:
      - "5432:5432"
    volumes:
      - pethood_pgdata:/var/lib/postgresql/data

volumes:
  pethood_pgdata:
